server:
  port: 8901
log:
  level: debug
hystrix:
    command:
      default:
        execution:
          isolation:
            thread:
              timeoutInMilliseconds: 10000
feign:
   client:
     config:
       default:
         connectTimeout: 5000 
        readTimeout: 10000 
